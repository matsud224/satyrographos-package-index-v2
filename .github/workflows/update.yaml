name: update-packege-data

on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: amutake/satysfi:latest
    steps:
      - name: Prepare apt
        run: apt update
      - name: Install packages
        run: |
          apt install -y xz-utils ruby
          gem install origami slack-incoming-webhooks
      - name: Install git
        run: |
          apt install -y software-properties-common
          add-apt-repository -y ppa:git-core/ppa
          apt update -y
          apt install -y git
          git --version
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Check diffs and update
        env:
          WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          GH_API_TOKEN: ${{ secrets.GH_API_TOKEN }}
        run: |
          export HOME=/root
          eval $(opam env)
          opam update
          opam install satyrographos-snapshot-stable || true # workaround
          satyrographos install
          cd repo2json
          cp -rL $HOME/.satysfi/dist/docs .
          cp -rL $HOME/.satysfi/dist/fonts .
          find ./docs -name "*.pdf" -type f -exec ../scripts/remove-pdf-id.rb {} \;
          git clone https://github.com/na4zagin3/satyrographos-repo.git
          make
          dune exec ./repo2json.exe packages.json snapshots.json
          cd ..
          scripts/add-github-repo-info.rb src/data/packages.json $GH_API_TOKEN
          rm -rf static/docs
          cp -rL repo2json/docs static/docs
          scripts/update-check.sh
